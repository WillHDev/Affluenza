type Playlist {
  id: ID!
  title: String!
  songs: [Song]!
  favorite: Boolean!
}

input UpdatedPlaylist {
  id: ID!
  title: String
  songs: [ID!]
  favorite: Boolean
}

input NewPlaylist {
  title: String
  songs: [ID!]
  favorite: Boolean
}
